import { PercentageQualityPipe } from './percentage-quality.pipe';

describe('PercentageQualityPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageQualityPipe();
    expect(pipe).toBeTruthy();
  });
});
