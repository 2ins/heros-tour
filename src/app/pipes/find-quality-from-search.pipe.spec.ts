import { FindQualityFromSearchPipe } from './find-quality-from-search.pipe';

describe('FindQualityFromSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FindQualityFromSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
