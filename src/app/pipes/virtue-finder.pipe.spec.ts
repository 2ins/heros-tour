import { VirtueFinderPipe } from './virtue-finder.pipe';

describe('VirtueFinderPipe', () => {
  it('create an instance', () => {
    const pipe = new VirtueFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
