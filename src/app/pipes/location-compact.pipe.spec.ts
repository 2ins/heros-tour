import { LocationCompactPipe } from './location-compact.pipe';

describe('LocationCompactPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationCompactPipe();
    expect(pipe).toBeTruthy();
  });
});
