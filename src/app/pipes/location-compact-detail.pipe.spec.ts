import { LocationCompactDetailPipe } from './location-compact-detail.pipe';

describe('LocationCompactDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationCompactDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
