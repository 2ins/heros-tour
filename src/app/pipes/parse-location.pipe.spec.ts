import { ParseLocationPipe } from './parse-location.pipe';

describe('ParseLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
