import { FilterByVirtuePipe } from './filter-by-virtue-pipe.pipe';

describe('FilterByVirtuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByVirtuePipe();
    expect(pipe).toBeTruthy();
  });
});
