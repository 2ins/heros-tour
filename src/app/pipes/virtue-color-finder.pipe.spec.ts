import { VirtueColorFinderPipe } from './virtue-color-finder.pipe';

describe('VirtueColorFinderPipe', () => {
  it('create an instance', () => {
    const pipe = new VirtueColorFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
